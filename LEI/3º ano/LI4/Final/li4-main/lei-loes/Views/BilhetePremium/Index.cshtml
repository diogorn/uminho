@model IEnumerable<lei_loes.Models.BilhetePremiumModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataAquisicao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Usado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataAquisicao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Usado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BilheteID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BilheteID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BilheteID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
