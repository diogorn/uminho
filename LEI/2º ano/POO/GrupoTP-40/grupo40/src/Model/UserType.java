package Model;

/*
 * Enumeração que representa o tipo de utilizadorm, apenas usada para efeitos de controller uma vez que pode ser sunstituida por "instanceof x"
 */
public enum UserType {
    PROFISSIONAL,
    AMADOR,
    OCASIONAL,
    OUTRO;
}
